name: fluttercamera
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  camera: ^0.11.0 # カメラパッケージ
  path_provider: ^2.1.1 # ファイルパス取得用パッケージ
  path: ^1.9.0 # ファイルパス操作用パッケージ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
